import { asc, eq, sql } from "drizzle-orm";import { ArrowLeft, Plus } from "lucide-react";import Link from "next/link";import { db } from "@/db/client";import { purchaseOrders, suppliers } from "@/db/schema";import { requirePermission } from "@/modules/auth/authorization";
export default async function SuppliersPage(){const access=await requirePermission("purchasing:read");const rows=await db.select({id:suppliers.id,name:suppliers.name,contact:suppliers.contactName,phone:suppliers.phone,email:suppliers.email,terms:suppliers.paymentTermsDays,active:suppliers.active,orders:sql<number>`count(${purchaseOrders.id})::int`}).from(suppliers).leftJoin(purchaseOrders,eq(purchaseOrders.supplierId,suppliers.id)).where(eq(suppliers.businessId,access.business.id)).groupBy(suppliers.id).orderBy(asc(suppliers.name));return <><header className="topbar"><div><Link className="back-link" href="/purchasing"><ArrowLeft size={15}/> Purchasing</Link><h1>Suppliers</h1><p>Supplier contacts, payment terms, and purchasing history.</p></div><Link className="button primary inline-button" href="/purchasing/suppliers/new"><Plus size={17}/> Add supplier</Link></header><main className="page"><section className="surface table-surface"><table><thead><tr><th>Supplier</th><th>Contact</th><th>Phone</th><th>Terms</th><th>Orders</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><strong>{x.name}</strong><small>{x.email||"No email"}</small></td><td>{x.contact||"—"}</td><td>{x.phone||"—"}</td><td>{x.terms?`${x.terms} days`:"Due immediately"}</td><td>{x.orders}</td><td><span className={x.active?"pill active":"pill"}>{x.active?"Active":"Inactive"}</span></td></tr>)}</tbody></table>{!rows.length&&<div className="empty-state">No suppliers yet.</div>}</section></main></>}
